name: Sync Galician metadata

on:
  push:
    branches:
      - release/0.9.0-beta
    paths:
      - .github/workflows/sync-metadata-galician.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          ref: release/0.9.0-beta

      - name: Update catalog
        run: python3 scripts/update_catalog_galician.py

      - name: Generate readable catalog
        run: python3 scripts/generate_catalog.py

      - name: Update README
        run: python3 scripts/update_readme_galician.py

      - name: Validate metadata
        run: |
          python3 - <<'PY'
          import json
          from pathlib import Path
          catalog = json.loads(Path('catalog.json').read_text(encoding='utf-8'))
          assert catalog['project']['supported_locale_count'] == 38
          for project in catalog['supported_projects']:
              assert 'gl_es' in project['languages'], project['id']
          readme = Path('README.md').read_text(encoding='utf-8')
          assert '38 langues' in readme or 'trente-huit langues' in readme
          assert 'Galicien (`gl_es`)' in readme
          assert 'Pour le galicien :' in readme
          generated = Path('CATALOG.md').read_text(encoding='utf-8')
          assert '`gl_es`' in generated
          print('Galician metadata validation passed: 38 locales')
          PY

      - name: Commit metadata
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add catalog.json CATALOG.md README.md
          if git diff --cached --quiet; then
            echo 'No Galician metadata changes to commit.'
            exit 0
          fi
          git commit -m 'Document Galician localization in 0.9.0 beta'
          git fetch origin release/0.9.0-beta
          git rebase origin/release/0.9.0-beta
          git push origin HEAD:release/0.9.0-beta
