name: Refine Kabyle localization

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  paused-for-sharding-update:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Workflow update staging commit; sharded refinement follows.'
