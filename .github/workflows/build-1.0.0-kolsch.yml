name: Build NeoOrigins Localization 1.0.0 Kölsch staging

on:
  push:
    branches:
      - release/1.0.0-kolsch
    paths:
      - 'src/**/ksh.json'
      - 'scripts/finalize_kolsch_metadata.py'
      - '.github/workflows/build-1.0.0-kolsch.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: mc-1.21.1
            java: '21'
            gradle: '8.14.3'
            minecraft: '1.21.1'
            minecraft_range: '[1.21.1]'
            neoforge: '21.1.248'
            loader_range: '[1,)'
            parchment: 'true'
            pack_format: '34'
            source_variant: mc1_21_1
            include_addon_translations: 'true'
            include_121_batch_translations: 'true'
            include_kabyle_121_translations: 'true'
            include_26_1_translations: 'false'
            include_26_2_translations: 'false'
            expected_delta: neoorigins_121_batch1
            expected_files: '27'
            mod_version: 1.0.0-beta+1.21.1
          - target: mc-26.1.x
            java: '25'
            gradle: '9.4.1'
            minecraft: '26.1.2'
            minecraft_range: '[26.1,26.2)'
            neoforge: '26.1.2.29-beta'
            loader_range: '[4,)'
            parchment: 'false'
            pack_format: '84'
            source_variant: mc26_1
            include_addon_translations: 'false'
            include_121_batch_translations: 'true'
            include_kabyle_121_translations: 'false'
            include_26_1_translations: 'true'
            include_26_2_translations: 'false'
            expected_delta: neoorigins_26_1
            expected_files: '17'
            mod_version: 1.0.0-beta+26.1
          - target: mc-26.2
            java: '25'
            gradle: '9.4.1'
            minecraft: '26.2'
            minecraft_range: '[26.2,26.3)'
            neoforge: '26.2.0.3-beta'
            loader_range: '[4,)'
            parchment: 'false'
            pack_format: '84'
            source_variant: mc26_1
            include_addon_translations: 'false'
            include_121_batch_translations: 'false'
            include_kabyle_121_translations: 'false'
            include_26_1_translations: 'false'
            include_26_2_translations: 'true'
            expected_delta: neoorigins_26_2
            expected_files: '17'
            mod_version: 1.0.0-beta+26.2

    name: Build ${{ matrix.target }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          ref: release/1.0.0-kolsch

      - name: Set up Java ${{ matrix.java }}
        uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: ${{ matrix.java }}

      - name: Set up Gradle ${{ matrix.gradle }}
        uses: gradle/actions/setup-gradle@v6
        with:
          gradle-version: ${{ matrix.gradle }}
          cache-disabled: true

      - name: Validate localization JSON
        run: python3 scripts/validate.py

      - name: Build pinned target
        run: |
          gradle build --stacktrace \
            "-Pjava_version=${{ matrix.java }}" \
            "-Puse_parchment=${{ matrix.parchment }}" \
            "-Presource_pack_format=${{ matrix.pack_format }}" \
            "-Psource_variant=${{ matrix.source_variant }}" \
            "-Pinclude_addon_translations=${{ matrix.include_addon_translations }}" \
            "-Pinclude_121_batch_translations=${{ matrix.include_121_batch_translations }}" \
            "-Pinclude_turkish_split_translations=true" \
            "-Pinclude_kabyle_121_translations=${{ matrix.include_kabyle_121_translations }}" \
            "-Pinclude_26_1_translations=${{ matrix.include_26_1_translations }}" \
            "-Pinclude_26_2_translations=${{ matrix.include_26_2_translations }}" \
            "-Pminecraft_version=${{ matrix.minecraft }}" \
            "-Pminecraft_version_range=${{ matrix.minecraft_range }}" \
            "-Pneo_version=${{ matrix.neoforge }}" \
            "-Ploader_version_range=${{ matrix.loader_range }}" \
            "-Pmod_version=${{ matrix.mod_version }}"

      - name: Inspect Kölsch files in built JAR
        env:
          TARGET: ${{ matrix.target }}
          EXPECTED_DELTA: ${{ matrix.expected_delta }}
          EXPECTED_FILES: ${{ matrix.expected_files }}
          INCLUDE_ADDONS: ${{ matrix.include_addon_translations }}
        run: |
          python3 - <<'PY'
          import os
          from pathlib import Path
          from zipfile import ZipFile

          jars = [p for p in Path('build/libs').glob('*.jar') if not p.name.endswith('-sources.jar')]
          if len(jars) != 1:
              raise SystemExit(f'Expected exactly one runtime JAR, found {[p.name for p in jars]}')
          jar = jars[0]
          with ZipFile(jar) as zf:
              names = zf.namelist()
              locale_files = sorted(n for n in names if n.endswith('/lang/ksh.json'))

          target = os.environ['TARGET']
          expected_delta = os.environ['EXPECTED_DELTA']
          expected_files = int(os.environ['EXPECTED_FILES'])
          include_addons = os.environ['INCLUDE_ADDONS'] == 'true'

          def present(fragment):
              return any(fragment in name for name in locale_files)

          missing_common = [
              f'neoorigins_ksh_common_{index:02d}/lang/ksh.json'
              for index in range(1, 17)
              if not present(f'neoorigins_ksh_common_{index:02d}/lang/ksh.json')
          ]
          if missing_common:
              raise SystemExit(f'Missing Kölsch common entries: {missing_common}')
          if not present(f'{expected_delta}/lang/ksh.json'):
              raise SystemExit(f'Missing expected Kölsch delta: {expected_delta}')

          addon_namespaces = [
              'medievalorigins', 'ibarnorigins', 'origins_fantasy',
              'origins_backgrounds', 'origins_backgrounds_two',
              'origins_backgrounds_iss', 'origins_furries',
              'origins_classes_ex', 'origins_classes_iss', 'originsmodernui',
          ]
          addon_entries = [f'{ns}/lang/ksh.json' for ns in addon_namespaces]
          if include_addons:
              missing_addons = [entry for entry in addon_entries if not present(entry)]
              if missing_addons:
                  raise SystemExit(f'Missing Kölsch add-on entries: {missing_addons}')
              if present('neoorigins_26_1/lang/ksh.json') or present('neoorigins_26_2/lang/ksh.json'):
                  raise SystemExit('1.21.1 JAR contains a 26.x Kölsch delta')
          else:
              wrongly_included = [entry for entry in addon_entries if present(entry)]
              if wrongly_included:
                  raise SystemExit(f'26.x JAR contains Kölsch add-ons: {wrongly_included}')
              if present('neoorigins_121_batch1/lang/ksh.json'):
                  raise SystemExit('26.x JAR contains the Kölsch 1.21.1 delta')
              other_delta = 'neoorigins_26_2' if expected_delta == 'neoorigins_26_1' else 'neoorigins_26_1'
              if present(f'{other_delta}/lang/ksh.json'):
                  raise SystemExit(f'{target} JAR contains wrong Kölsch delta {other_delta}')

          if len(locale_files) != expected_files:
              raise SystemExit(f'{target}: expected {expected_files} ksh files, got {len(locale_files)}: {locale_files}')
          print(f'Kölsch ksh audit: {target} = {len(locale_files)} files; JAR={jar.name}; delta={expected_delta}')
          PY

      - name: Upload built JAR
        uses: actions/upload-artifact@v7
        with:
          name: neoorigins-localization-1.0.0-kolsch-${{ matrix.target }}-${{ github.sha }}
          path: build/libs/*.jar
          if-no-files-found: error

  finalize-metadata:
    name: Finalize locale 90 metadata
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout latest staging branch
        uses: actions/checkout@v7
        with:
          ref: release/1.0.0-kolsch
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: Finalize Kölsch metadata
        run: python scripts/finalize_kolsch_metadata.py

      - name: Validate metadata and JSON
        run: |
          python scripts/validate.py
          python - <<'PY'
          import json
          from pathlib import Path

          catalog = json.loads(Path('catalog.json').read_text(encoding='utf-8'))
          assert catalog['project']['supported_locale_count'] == 90

          def count_locale(node):
              if isinstance(node, dict):
                  here = 1 if 'ksh' in node and isinstance(node['ksh'], dict) and node['ksh'].get('status') == 'supported' else 0
                  return here + sum(count_locale(v) for v in node.values())
              if isinstance(node, list):
                  return sum(count_locale(v) for v in node)
              return 0

          assert count_locale(catalog) == 11
          neo = next(p for p in catalog['supported_projects'] if p['id'] == 'neoorigins')
          ns = neo['compatibility']['fallback_namespaces']
          assert ns['kolsch_common_glob'] == 'neoorigins_ksh_common_*'
          assert ns['kolsch_mc_1_21_1'] == 'neoorigins_121_batch1'
          readme = Path('README.md').read_text(encoding='utf-8')
          catalogue = Path('CATALOG.md').read_text(encoding='utf-8')
          assert 'Kölsch (`ksh`)' in readme
          assert 'Kölsch/Ripoarisch (`ksh`)' in catalogue
          print('Kölsch metadata audit: locale 90; ksh across 11 projects; namespaces OK')
          PY

      - name: Commit validated metadata
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add README.md CATALOG.md catalog.json
          if git diff --cached --quiet; then
            echo 'No Kölsch metadata changes to commit.'
            exit 0
          fi
          git commit -m 'Finalize Kölsch locale 90 metadata'
          git fetch origin release/1.0.0-kolsch
          git rebase origin/release/1.0.0-kolsch
          git push origin HEAD:release/1.0.0-kolsch
