name: Sync 0.9.0 Serbian Cyrillic metadata

on:
  push:
    branches:
      - release/0.9.0-beta
    paths:
      - 'src/main/resources/resourcepacks/fallback_localizations/assets/**/sr_sp.json'
      - 'scripts/update_catalog_serbian.py'
      - 'scripts/update_readme_serbian.py'
      - '.github/workflows/sync-0.9.0-serbian-metadata.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          ref: release/0.9.0-beta

      - name: Update catalog and README
        run: |
          python3 scripts/update_catalog_serbian.py
          python3 scripts/generate_catalog.py
          python3 scripts/update_readme_serbian.py

      - name: Validate metadata
        run: |
          python3 - <<'PY'
          import json
          from pathlib import Path
          catalog = json.loads(Path('catalog.json').read_text(encoding='utf-8'))
          if catalog['project']['supported_locale_count'] != 31:
              raise SystemExit('catalog locale count is not 31')
          for project in catalog['supported_projects']:
              if project['languages'].get('sr_sp', {}).get('status') != 'supported':
                  raise SystemExit(f"sr_sp missing from {project['id']}")
          readme = Path('README.md').read_text(encoding='utf-8')
          if 'Serbe cyrillique (`sr_sp`)' not in readme or 'Les trente et une langues' not in readme:
              raise SystemExit('README Serbian Cyrillic metadata missing')
          if 'Couverture CS / HU / JA / KO / UK / ID / SV / DA / FI / NO / RO / EL / BG / VI / AR / HE / TH / SK / SL / HR / SR' not in readme:
              raise SystemExit('README Serbian Cyrillic coverage header missing')
          print('Serbian Cyrillic metadata validation passed')
          PY

      - name: Commit synchronized metadata
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add catalog.json CATALOG.md README.md
          if git diff --cached --quiet; then
            echo 'No metadata changes to commit.'
            exit 0
          fi
          git commit -m 'Document Serbian Cyrillic localization in 0.9.0 beta'
          git fetch origin release/0.9.0-beta
          git rebase origin/release/0.9.0-beta
          git push origin HEAD:release/0.9.0-beta
